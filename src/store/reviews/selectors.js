export const selectReviewId = (state) => state.reviewsReducer.reviewId;

export const selectReviews = (state) => state.reviewsReducer.reviews;

export const selectCurReview = (state) => state.reviewsReducer.curReview;
